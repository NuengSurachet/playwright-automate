{
  "name": "playwright-automate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:single:A": "set TEST_ENV=A && npx playwright test --workers 1 --headed",
    "test:single:B": "set TEST_ENV=B && npx playwright test --workers 1 --headed",
    "test:parallel": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.14.2",
    "@types/xlsx": "^0.0.36"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "xlsx": "^0.18.5"
  }
}
